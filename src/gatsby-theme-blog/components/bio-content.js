import React, { Fragment } from "react"
import { Styled } from "theme-ui"

export default () => (
  <Fragment>
    A new Action every day during the first 25 days of December. A project by
    {` `}
    <Styled.a href="https://lannonbr.com/">Benjamin Lannon</Styled.a>.
  </Fragment>
)
